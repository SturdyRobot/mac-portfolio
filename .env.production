# Scope tool → AI-summary proxy (a PUBLIC Cloudflare Worker URL — not a secret).
# The Groq API key lives only inside the Worker; see worker/README.md.
# Vite bakes this into the production build automatically.
VITE_SCOPE_PROXY=https://scope-proxy.hayewire.workers.dev
